This is another Java app I did for school. It’s an app that basically tells you the weather conditions in a the given area and displays them with GUI. It also enters the data into a MySQL database via JDBC connection. 

App was prepared with Eclipse IDE.
